Use orchestration mode for this request.

If orchestration mode is not enabled, tell me to run `/orch` first.

Load the latest orchestration run artifacts, inspect the latest plan and changed files, and run the reviewer workflow again. Return the verdict, critical issues, warnings, and suggested rerouting if needed.
