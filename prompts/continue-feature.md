Use orchestration mode for this request.

If orchestration mode is not enabled, tell me to run `/orch` first.

Continue work on the current feature using the latest matching plan version if one exists. If the feature already has a plan, create a new version instead of overwriting the old one. Then continue through planning, execution, and review as needed.
